create table PARKING_ORDER (
    ID varchar(100) not null,
    NAME varchar(100),
    CAR_NUMBER varchar(100),
    CREATION_TIME DATE,
    END_TIME DATE,
    STATUS boolean,
    PARKING_LOT_ID varchar(100)
);